# ========== 前端（Vercel / EdgeOne Pages）环境变量 ==========
# 见根目录 README.md 与 DEPLOY.md 第 1 节完整说明

# n8n 工作流 Webhook 地址（必须为外网可访问地址）
N8N_REVIEW_URL=https://n8n.example.com/webhook/like-review
N8N_REGISTER_URL=https://n8n.example.com/webhook/like-register
# 轻量查重（Step2 填写信息时即时调用，只查手机号是否已参与）
N8N_CHECK_DUPLICATE_URL=https://n8n.example.com/webhook/check-duplicate

# 前端接口鉴权密钥：与 n8n 端 API_SECRET_PUBLIC 同值（服务端附加 x-api-key，不暴露给浏览器）
# 未配置 API_SECRET_PUBLIC 时服务端回退旧 API_SECRET（仅过渡兼容；配置稳定后请移除旧值）
API_SECRET_PUBLIC=请改成一段足够长的随机字符串

# （过渡）旧共用密钥：仅在 API_SECRET_PUBLIC 未配置时被回退使用
# API_SECRET=
